import CreateMessage from '../CreateMessage/CreateMessage';
import Feed from '../Feed/Feed';
import './App.css';

function App() {
  return (
    <div className="App">
      <CreateMessage/>
      <Feed/>
    </div>
  );
}

export default App;
